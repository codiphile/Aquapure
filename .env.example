# Google Maps API key
# Make sure to enable both the Maps JavaScript API and Places API in Google Cloud Console
# Also ensure that the key has proper API restrictions (HTTP referrers) for localhost
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Gemini API key for AI analysis
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# Database connection (if using an external database)
# DATABASE_URL=your_database_connection_string 